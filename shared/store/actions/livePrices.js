import { createAction } from 'redux-actions';

export const setReaction = createAction('SET_REACTION');

export const setStart = createAction('SET_START');
export const setRowsPerPage = createAction('SET_ROWS_PER_PAGE');
export const reorder = createAction('REORDER');
