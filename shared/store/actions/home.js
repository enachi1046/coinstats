import { createAction } from 'redux-actions';

export const onChangeTitle = createAction('CHANGE_TITLE');
export const onChangeMetas = createAction('CHANGE_METAS');
export const setActiveNavBar = createAction('SET_ACTVIVE_NAV_BAR');
